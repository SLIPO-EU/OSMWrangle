PREFIX slipo: <http://slipo.eu/def#> 
PREFIX geo: <http://www.opengis.net/ont/geosparql#>
PREFIX sf: <http://www.opengis.net/ont/sf#>

# Sanity check: SPARQL query to count the number of triples in the generated RDF graph

SELECT (COUNT(*) AS ?triple_count)  {?s ?p ?o}